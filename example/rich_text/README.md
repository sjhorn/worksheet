# worksheet rich_text example

A standalone flutter example to demonstrate richtext including fonts. 

Be sure to enabled network client access

eg. for MacOS in macos/Runner/DebugProfile.entitlements add

```
<key>com.apple.security.network.client</key>
<true/>
```
