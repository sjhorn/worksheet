name: worksheet
description: "High-performance Flutter spreadsheet widget supporting large datasets, 10%-400% zoom, and GPU-optimized tile-based rendering."
version: 1.0.0
repository: https://github.com/shorn/worksheet
issue_tracker: https://github.com/shorn/worksheet/issues
topics:
  - spreadsheet
  - worksheet
  - excel
  - table
  - widget

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Regular.ttf
